
"use client";
// This component is not strictly needed as its functionality has been integrated into the page.tsx
// However, I'm creating it as an empty placeholder as per the plan to potentially refactor later.
// For now, course listing and management is handled directly in src/app/(app)/training-competence/page.tsx

export function CoursesList() {
  return null; 
}
